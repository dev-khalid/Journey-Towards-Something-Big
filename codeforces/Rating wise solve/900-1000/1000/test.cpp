#include<bits/stdc++.h>
using namespace std;
int dist(pair<int,int> &a,pair<int,int> &b) {
    return abs(a.first-b.first) + abs(a.second-b.second);
}
int main() {
    cout << 1;
    int n = 99;
    while(n--) {
        cout << 0;
    }
    cout << endl;
}
